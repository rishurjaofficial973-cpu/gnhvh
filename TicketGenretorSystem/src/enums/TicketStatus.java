package enums;

public enum TicketStatus {
    // ABSTRACTION
    // Enum use karke fixed states define kiye gaye hain
    // User ko sirf OPEN / CLOSED dikhta hai, internal string handling hide ho jati hai

    OPEN,
    IN_PROGRESS,
    CLOSED
}
